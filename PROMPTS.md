# Golden Prompts (Copy/Paste)

Each prompt returns:
- Top 3 endpoints + match reasons
- Payload skeleton + unknown required fields
- curl snippet

## Prompts

1. Create a pet
Expected top endpoint: `pets:createPet`

2. List all pets
Expected top endpoint: `pets:listPets`

3. Get pet by ID
Expected top endpoint: `pets:getPet`

4. List all orders
Expected top endpoint: `store-v2:listOrders`

5. Get an order by ID
Expected top endpoint: `store-v2:getOrder`

6. Rotate an API key
Expected top endpoint: `auth:rotateKey`

7. List API keys
Expected top endpoint: `auth:listKeys`

8. Create a cat payload
Expected top endpoint: `pets:createPet`

9. Create a dog payload
Expected top endpoint: `pets:createPet`

10. Show me the pet schema
Expected top endpoint: `pets:createPet`
